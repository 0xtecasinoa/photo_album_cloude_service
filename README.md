# らくらく写真台帳

工事写真の撮影から台帳作成・共有・電子納品までを、ひとつのクラウドで完結させる建設業向けサービスです。

現場で撮影した写真は、撮影日時・電子小黒板の情報とともにそのままクラウドへ。
AI が黒板や手書き看板の文字を読み取り、工事写真台帳の作成から Excel / PDF 出力、
国土交通省の電子納品要領に沿った成果品の書き出しまでを自動化します。

---

## 目次

- [主な機能](#主な機能)
- [技術スタック](#技術スタック)
- [動作環境](#動作環境)
- [セットアップ](#セットアップ)
- [環境変数](#環境変数)
- [データベース](#データベース)
- [テスト](#テスト)
- [ディレクトリ構成](#ディレクトリ構成)
- [権限モデル](#権限モデル)
- [電子納品への対応方針](#電子納品への対応方針)
- [現在の実装状況](#現在の実装状況)
- [デザイン](#デザイン)

---

## 主な機能

### 写真管理

- 撮影日時（EXIF `DateTimeOriginal`）を必ず保持し、台帳・一覧・並び替えの基準にします
- GPS 座標、カメラ機種、向き（Orientation）などの EXIF 情報を保存します
- SHA-256 による重複検出と、改ざん検知のための基準ハッシュを記録します
- 削除は論理削除。監査ログは削除後も残り、復元も可能です

### 電子小黒板（看板）

- 黒板テンプレートをブラウザ上で自由に設計できます
- 項目ごとに **文字サイズ・書体・太さ・文字色・背景色・字下げ・行間・縦横位置** を個別に設定できます
- ラベルと値のスタイルを別々に指定できます（実際の工事黒板の書式に合わせるため）
- 項目のデータ種別（項目カテゴリ）を選択できます
  - 手入力／選択肢から入力
  - 工事名・工事番号・発注者名・請負者名・施工場所（案件から自動入力）
  - 撮影年月日・撮影日時・撮影者・連番（撮影時に自動入力）
  - 工種・種別・細別・写真区分・撮影箇所・施工管理値
- 「（自動）」の項目は撮影時に自動で入り、現場では上書きできません
- 透過黒板（不透明度の調整）に対応しています
- テンプレートは管理者がロックでき、現場での書式崩れを防ぎます

### 手書き看板の取り込み

手書き看板を撮影した写真から OCR で項目を読み取り、黒板テンプレートの下書きを生成します。

手書き文字の判別精度は文字の大きさ・書き方・撮影環境により変動するため、
**「OCR の下書き → 人が確認・修正 → 確定」** というレビュー方式を採っています。
自動確定はしません。納品写真に誤った黒板が載ることを避けるための設計です。

### クラウド共有と権限管理

- 会社 → 現場 → アルバム → ファイルの階層でデータを管理します
- 権限は **38 種類の操作単位** で定義され、ロールに割り当てます
- 既定ロール：オーナー／管理者／現場監督／編集者／撮影者／閲覧者／協力会社
- 「写真は削除できるが台帳は削除できない」といった独自ロールも、コード変更なしで作成できます
- 協力会社など他社のユーザーは、明示的に付与された現場のみアクセスできます（権限の継承はありません）
- 期限付きの外部共有リンク（パスワード・ダウンロード可否・閲覧回数制限つき）を発行できます
- すべての操作は監査ログに追記され、「いつ・誰が・何を」を後から確認できます

### 出力

- **Excel（.xlsx）** — 既定の台帳書式で出力できるほか、発注者指定の `.xlsx` 雛形に
  `{{工事名}}` 形式のプレースホルダを書いておけば、その書式のまま差し込めます。
  発注者ごとに台帳の書式が違うため、書式を固定しない設計にしています
- **PDF** — 提出・印刷用。1 ページあたりの写真枚数を 1 / 3 / 4 / 6 枚から選択できます。
  Chromium の印刷機能で生成するため、日本語の禁則処理・字詰めが崩れません
- **電子納品（CALS/EC）** — `PHOTO/PIC`・`PHOTO/DRA` 構成、`PHOTO.XML`（Shift_JIS）を
  ZIP で出力します。署名が検証できない写真が含まれる場合は出力を停止します
- **Word（.docx）** — 未実装

---

## 技術スタック

| 区分 | 採用技術 |
| --- | --- |
| フレームワーク | Next.js 16（App Router） |
| 言語 | TypeScript |
| UI | React 19 / Tailwind CSS v4 |
| フォント | Zen Kaku Gothic New |
| 認証 | Auth.js v5（NextAuth） |
| データベース | PostgreSQL 17 |
| ORM | Drizzle ORM |
| ストレージ | S3 互換（開発時 MinIO、本番は S3 / Cloudflare R2 など） |
| 画像処理 | sharp / exifr |
| 帳票 | ExcelJS / pdf-lib |
| バリデーション | Zod |
| テスト | Node.js 標準テストランナー（`node:test`） |

---

## 動作環境

- Node.js 24 以上
- Docker / Docker Compose（開発用の PostgreSQL・MinIO を起動するため）

---

## セットアップ

### 1. 依存パッケージのインストール

```bash
npm install
```

### 2. 環境変数の設定

```bash
cp .env.example .env
```

`.env` を開き、`AUTH_SECRET` を生成した値に置き換えてください。

```bash
openssl rand -base64 32
```

### 3. データベースの起動

Docker が使える場合は PostgreSQL と MinIO（S3 互換ストレージ）を起動します。

```bash
docker compose up -d
```

MinIO の管理画面は <http://localhost:9001>（`minioadmin` / `minioadmin`）です。

> **Docker が使えない場合**
> `npm run dev:db` で、Docker なしに PostgreSQL を起動できます。
> PGlite（WebAssembly 版 PostgreSQL）を PostgreSQL のワイヤプロトコルで
> 公開するため、`DATABASE_URL` もアプリ側のコードも本番と同じまま使えます。
>
> ```bash
> npm run dev:db        # 127.0.0.1:5432 で待ち受け、データは .pgdata/
> ```
>
> ただし **同時接続は1本まで**です。開発サーバーを起動したまま psql などから
> 接続するとリセットされます。並行処理が必要な場合は通常の PostgreSQL を使い、
> `DB_POOL_MAX` で接続数を指定してください。

### 4. マイグレーションと初期データ

```bash
npm run db:migrate
npm run db:seed
```

シードを流すと、以下のアカウントでログインできます。

| メールアドレス | パスワード | 権限 |
| --- | --- | --- |
| `taro.yamada@example.com` | `password1234` | オーナー |
| `hanako.sato@example.com` | `password1234` | 編集者 |
| `ichiro.suzuki@example.com` | `password1234` | 閲覧者 |

### 5. 開発サーバーの起動

```bash
npm run dev
```

<http://localhost:3000> で起動します（`127.0.0.1` ではなく `localhost` を使ってください）。

### 補足：データベースなしで保護画面を確認する

実データベースを立てる前に画面や API を確認したい場合、検証用のセッション
トークンを発行できます（開発環境専用）。

```bash
TOKEN=$(node scripts/dev-session-token.mjs)
curl -b "authjs.session-token=$TOKEN" \
  "http://localhost:3000/api/projects/project-1/export/pdf?perPage=4" -o 台帳.pdf
```

---

## 環境変数

| 変数名 | 必須 | 説明 |
| --- | :---: | --- |
| `DATABASE_URL` | ○ | PostgreSQL の接続 URL |
| `AUTH_SECRET` | ○ | セッション署名用の秘密鍵（32 文字以上） |
| `AUTH_URL` | | アプリケーションの URL |
| `S3_ENDPOINT` | ○ | オブジェクトストレージのエンドポイント |
| `S3_REGION` | ○ | リージョン（既定：`ap-northeast-1`） |
| `S3_BUCKET` | ○ | バケット名 |
| `S3_ACCESS_KEY_ID` | ○ | アクセスキー |
| `S3_SECRET_ACCESS_KEY` | ○ | シークレットキー |
| `S3_FORCE_PATH_STYLE` | | MinIO 利用時は `true`。本番の S3 では不要 |
| `MAX_UPLOAD_BYTES` | | 1 ファイルあたりの上限（既定：50MB） |
| `GOOGLE_CLIENT_ID` | | Google ログインを使う場合のみ |
| `GOOGLE_CLIENT_SECRET` | | Google ログインを使う場合のみ |
| `DB_POOL_MAX` | | 接続プールの上限（既定：開発 1／本番 20） |
| `ANTHROPIC_API_KEY` | | 手書き看板 OCR を利用する場合のみ |

環境変数は起動時に Zod で検証されます。不足や不正があれば、
実行中ではなく起動時にエラーとして表示されます。

### Google ログインを有効にする（任意）

1. Google Cloud Console で OAuth クライアント ID（ウェブアプリケーション）を作成
2. 承認済みのリダイレクト URI に次を追加
   `http://localhost:3000/api/auth/callback/google`
3. `.env` に以下を設定

```bash
GOOGLE_CLIENT_ID="..."
GOOGLE_CLIENT_SECRET="..."
```

未設定のあいだは、ログイン・新規登録画面に Google のボタンは表示されません。
押しても必ず失敗するボタンを見せないためです。

初回サインイン時には、組織と7つの既定ロールが自動で作成され、
そのユーザーはオーナーになります。

---

## データベース

スキーマは `src/db/schema/` に定義されています（全 16 テーブル）。

```bash
npm run db:generate   # スキーマからマイグレーション SQL を生成
npm run db:migrate    # マイグレーションを適用
npm run db:push       # 開発用：スキーマを直接反映
npm run db:studio     # Drizzle Studio を起動
```

主なテーブル：

| テーブル | 役割 |
| --- | --- |
| `organizations` | 会社（テナントの起点） |
| `users` | 利用者 |
| `roles` | ロールと権限（会社ごとに定義） |
| `projects` | 工事・現場 |
| `project_members` | 現場ごとのアクセス権 |
| `albums` | 工事写真台帳 |
| `photos` | 写真（撮影日時・EXIF・黒板情報・電子納品項目） |
| `drawings` | 図面・参考図 |
| `blackboard_templates` | 電子小黒板テンプレート |
| `blackboard_imports` | 手書き看板の OCR 取り込み |
| `share_links` | 期限付き外部共有リンク |
| `audit_logs` | 監査ログ（追記専用） |

---

## テスト

```bash
npm test           # ユニットテスト（41 件）
npm run typecheck  # 型チェック
npm run lint       # Lint
npm run smoke      # 画面操作のスモークテスト（要: サーバー起動 + Chrome）
npm run smoke:auth # 認証まわりのスモークテスト（要: db:seed 実行済み）
```

`npm run smoke` は実際のブラウザで画面を操作し、黒板エディタの編集・写真の選択・
台帳の枚数切替・電子納品の適合チェックなどが動作することを確認します。

`npm run smoke:auth` は新規登録からログアウトまでを通しで確認します。
セッションを偽造せず実際にフォームを送信するため、認証の不具合を取りこぼしません
（新規登録の確認のため、実行のたびにアカウントが1件増えます）。

> **注意**
> 開発サーバーには必ず `http://localhost:3000` でアクセスしてください。
> `127.0.0.1` だと HMR の WebSocket が接続できず、React のハイドレーションが
> 行われないため、画面上のすべての操作が無反応になります。

> **画像を差し替えても表示が変わらないとき**
> `next/image` は最適化結果を **`.next/dev/cache/images`** にキャッシュします
> （`.next/cache` ではありません）。同じファイル名のまま中身だけ差し替えると
> 古い画像が表示され続けるため、次のように消してから再起動してください。
>
> ```bash
> rm -rf .next/dev/cache/images
> ```
>
> キャッシュは幅ごとに保存されます。ブラウザが実際に読むのは srcset 中の
> 1 つ（多くは `w=640`）なので、別の幅だけ確認しても気づけません。

ユニットテストは写真処理と出力処理を対象としています。
特に **EXIF の撮影日時の扱い** は重点的に検証しています。

EXIF の日時にはタイムゾーン情報がありません。
一般的なライブラリはこれを「サーバーのローカルタイムゾーン」として解釈するため、
デプロイ先によって撮影年月日がずれ、深夜に撮影した写真が前日の台帳に入ってしまいます。
本実装では EXIF の文字列を自前で解析し、日本時間として確定させています
（カメラが UTC オフセットを記録している場合はそちらを優先します）。

---

## ディレクトリ構成

```
src/
├── app/
│   ├── (app)/            認証後の画面（ダッシュボード・台帳・権限・テンプレート・設定）
│   ├── (auth)/           ログイン・アカウント作成
│   ├── api/auth/         認証 API
│   ├── contact/          お問い合わせ
│   └── page.tsx          サービス紹介ページ
├── components/
│   ├── app/              アプリ共通（サイドバー・ヘッダー）
│   ├── blackboard/       電子小黒板エディタ
│   ├── marketing/        紹介ページ用
│   ├── photos/           写真一覧・台帳
│   └── ui/               UI プリミティブ
├── db/
│   └── schema/           Drizzle スキーマ
├── lib/
│   ├── acl/              権限定義と判定
│   ├── audit/            監査ログ
│   ├── blackboard/       黒板レイアウトのスキーマ
│   ├── photo/            EXIF 解析・画像処理
│   └── storage/          S3 互換ストレージ
└── types/
```

---

## 権限モデル

アプリケーション内のどこにも「管理者かどうか」という判定はありません。
すべて「その操作の権限を持っているか」で判定します。

```ts
// 現場に対する権限を解決し、必要な権限がなければ例外を投げる
await requireProjectCapability(userId, projectId, 'photo.delete');
```

自社の現場か、他社（協力会社）の現場かで挙動が異なります。

- **自社の現場** — 現場ごとのロールが優先。なければ会社全体の既定ロールが適用されます
- **他社の現場** — 明示的に付与された権限のみ。継承は一切ありません

この区別により、協力会社のユーザーが権限を付与されていない現場を見ることはできません。

---

## 電子納品への対応方針

公共工事での利用を前提に、以下を設計に織り込んでいます。

- **小黒板情報電子化（JACIC）** — 黒板情報を JPEG に埋め込み、改ざん検知の署名を保持します
  - 署名の検証状態は写真ごとに `unsigned` / `pending` / `valid` / `invalid` / `error` で記録します
  - 電子納品モードが ON のとき、検証できない写真が含まれていると出力を自動停止します
- **デジタル写真管理情報基準** — `PHOTO/PIC`・`PHOTO/DRA` のフォルダ構成、`PHOTO.XML` の出力
- **有効画素数** — 黒板の文字が判読できることを目安に、100 万画素程度を推奨値としてアップロード時に判定します
- **原本の非改変** — 撮影された元データは再エンコードせず保持します。
  再圧縮は改ざん検知の署名を壊すうえ、写真の編集にあたるためです

---

## 現在の実装状況

### 実装済み

- 画面（Figma デザインに基づく全 15 ルート）
- データベーススキーマ（全 16 テーブル）とマイグレーション
- 権限モデル（38 権限・7 ロール）と判定処理
- 監査ログ
- 認証（Auth.js v5）
  - 新規登録（組織と7つの既定ロールを同時に作成し、そのままログイン）
  - ログイン・ログアウト、ルート保護
  - Google ログイン（環境変数が設定されている場合のみ表示）
  - 入力検証とエラー表示、callbackUrl の検証（外部URLへの遷移を遮断）
- EXIF 解析（撮影日時・GPS・向き）とテスト
- 画像処理（ハッシュ・サムネイル・表示用画像・有効画素数判定）とテスト
- 電子小黒板テンプレートエディタ（動作確認済み）
- S3 互換ストレージ層
- **Excel 出力**（既定書式＋発注者雛形への差し込み）
- **PDF 出力**（1/3/4/6 枚組、日本語組版）
- **電子納品出力**（PHOTO.XML／Shift_JIS、PIC・DRA 構成、適合チェック付き）
- 出力 API（`/api/projects/[projectId]/export/[format]`）と監査ログ記録
- 画面操作のスモークテスト（`npm run smoke`、16 項目）

### 未実装

- 画面から実データベースへの接続（一部のみ）
  - ヘッダー・サイドバーはログイン中のユーザーと組織を実データで表示します
  - 一覧・台帳などの本文はまだ `src/lib/demo-data.ts` を参照しています。
    差し替え箇所は `src/lib/export/ledger-source.ts` の 2 関数です
- 写真アップロードの API 実装
- Word（.docx）出力
- 手書き看板の OCR 処理本体
- メンバー招待・共有リンク発行の実処理（権限モデル自体は実装済み）
- スマートフォン用の撮影アプリ（別途 React Native / Expo で実装予定）

> **撮影アプリについて**
> 「撮影したら自動でアップロードされる」機能は、Web アプリ単体では実現できません。
> iOS・Android のいずれも、Web ページがバックグラウンドで写真ライブラリを読むことを許可していないためです。
> また電子小黒板は、改ざん検知に対応するために **撮影時に写真へ合成する**必要があります。
> このため撮影機能は専用アプリとして実装します。

---

## デザイン

Figma：Construction Photo Album Cloud Service

配色・余白・書体は Figma の定義値をそのまま取り込んでいます。
デザイントークンは `src/app/globals.css` に集約されているため、
配色の変更はこのファイルの編集だけで全画面に反映されます。

| 用途 | 値 |
| --- | --- |
| ブランド（紺） | `#1E3A8B` |
| アクセント（金） | `#F0AE1E` |
| 成功・AI 抽出（緑） | `#2E7E61` |
| 黒板 | `#132A22` |
| 黒板の項目名 | `#8EFF9F` |
| 書体 | Zen Kaku Gothic New |

---

## ライセンス

本リポジトリは非公開のプロダクトコードです。無断での複製・再配布を禁じます。
